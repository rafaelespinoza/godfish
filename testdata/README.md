This directory contains migrations testdata.

If the DB has a more specific form of SQL, then find its migrations in the
corresponding subdirectory. Otherwise, use the files in the [default](./default)
subdirectory.
